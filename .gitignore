data/
.env
__pycache__/
.venv/
*.pyc
.claude/settings.local.json
.claude/worktrees/
.tmp
.vscode

# Firebase service-account keys. These belong OUTSIDE the repo (this one is
# public); the guard is only for a stray copy. Deliberately narrow: it must not
# swallow firebase.json, which is ordinary CLI config and belongs in git.
*-firebase-adminsdk-*.json
firebase-sa.json
